//listen for when the user clicks on the icon
    $('.hamburger').on('click', function(){
      //if menu is closed, slide down

      //if menus is open, slide up
      $('.menu').slideToggle();
    });
